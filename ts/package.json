{
  "name": "chaos-mesh-lite",
  "version": "0.1.0",
  "description": "Chaos experiments as CI-runnable SLO assertions - TypeScript port.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
